apiVersion: v1
kind: PersistentVolume
metadata:
  namespace: gold
  name: default-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  nfs:
    path: /exports
    # FIX: fill with the right ip after creating the nfs server
    server: 172.17.0.2